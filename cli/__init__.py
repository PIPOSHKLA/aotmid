"""AOT Stock Network — CLI toolset.

Usage:
    python -m cli fetch          Fetch all data sources
    python -m cli preprocess     Run preprocessing pipeline
    python -m cli train          Train all models
    python -m cli export         Generate figures and report
    python -m cli dashboard      Launch Streamlit dashboard
    python -m cli run-all        Execute full pipeline end-to-end
"""
